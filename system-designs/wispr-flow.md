<!--
Generated by system-design-generator
Target: Wispr Flow
Type: real
Generated: 2026-08-02
Research sources: 6 primary/secondary sources consulted (Wispr Flow blog, Baseten customer case study, Wispr Flow docs/privacy policy, funding/press coverage)
Artifact: https://claude.ai/code/artifact/abe53091-5a2e-40ea-bea4-8322f4584f9a
-->

# Wispr Flow

*An LLM-native, OS-layer voice dictation tool that turns speech into properly formatted text in any application — not just a faster microphone button.*

**By the numbers:** ~$10M ARR (2025) · $700M valuation (Nov 2025), reportedly in talks near $2B (2026) · 2.5M+ downloads · 270 Fortune 500 customers · 70% 12-month retention · <700ms p99 end-to-end latency

**Topics covered:** Requirements & Scope · Capacity Estimation · High-Level Architecture · APIs & Contracts · Data Model & Storage · Audio Capture & OS Integration · LLM Post-Processing Pipeline (not streaming ASR) · Context-Aware Formatting · Model Serving on Baseten/AWS · Privacy Modes & Zero Data Retention · Cross-Platform Divergence (Android vs. iOS vs. Desktop) · Scaling & Caching · Failures & Reliability · Recap & Interview Cheat Sheet

## 01. Requirements & Scope

**Core features**
- ✅ Real-time voice dictation, system-wide (not confined to one app)
- ✅ LLM-based cleanup: grammar correction, tone matching, personal phrasing adaptation
- ✅ Context-aware output formatting (an email reads like an email, a code comment reads like a code comment)
- ✅ Cross-platform: macOS, Windows, iOS, Android
- ✅ Privacy controls: Privacy Mode (zero data retention) and optional Private Cloud Sync

**Functional requirements**
- Capture audio from the OS microphone layer regardless of which foreground app has focus, and inject formatted text back into that app's input field.
- Detect the active application and relevant visible context so formatting adapts to destination (Slack vs. email vs. IDE).
- Process speech through an LLM-based correction/formatting pass rather than emitting a raw ASR transcript.
- Support enterprise trust requirements (SOC 2 Type II, HIPAA controls) for regulated customers *(sourced: [Baseten case study](https://www.baseten.co/resources/customers/wispr-flow/), Wispr Flow security FAQ)*.

**Non-functional requirements**
- Low perceived latency: end-to-end pipeline p99 < 700ms is the company's own stated target, prioritized over median latency *(sourced: Baseten case study — "we're optimizing the p99 experience for the p99 user")*.
- High transcription accuracy across accents, microphones, and background noise (independently reported ~92–97% depending on mic/environment).
- Strong data-handling guarantees given audio/screen-context is sent to third-party cloud infra.

**Out of scope**
- ❌ Fully offline/on-device transcription — Flow is cloud-only by design *(this is the opposite tradeoff from on-device competitors like Superwhisper)*.
- ❌ Meeting/multi-speaker transcription (Otter's category, not Flow's) — Flow is single-speaker, real-time dictation.
- ❌ A public developer API for third-party integration *(no evidence of one was found in research — treat as not currently offered)* *(inferred — not publicly confirmed)*.

> **💡 Interview Tip:** The single most interesting design decision here is architectural, not ML: Flow deliberately does **not** stream a live ASR transcript. It buffers, lets the LLM "understand what you meant," and only then renders text — trading a few hundred ms of perceived latency for dramatically fewer correction/backspace cycles. Framing your whole answer around that one tradeoff is a strong way to structure a live interview response.

## 02. Capacity Estimation

**Assumptions** *(daily-active-user figure is estimated from public download/retention data, not officially disclosed — flagged below)*
- ~2.5M cumulative downloads, 70% 12-month retention → assume ~400K–600K monthly active users *(inferred — not publicly confirmed)*
- Assume ~15% of MAU are daily dictation users → ~60K–90K DAU *(inferred — not publicly confirmed)*
- Assume average 25 dictation sessions/user/day, ~15 seconds of audio per session (typical for short dictation bursts)
- Audio encoded compactly for upload (~16kbps compressed speech-optimized codec assumed)

**Throughput / bandwidth**
```
75,000 DAU × 25 sessions/day × 15s audio × 16 kbps
= 75,000 × 25 × 15 × 16,000 bits ÷ 8 bits/byte
= 75,000 × 25 × 30,000 bytes
≈ 56.25 GB of audio uploaded per day
```
*Takeaway:* Per-session payloads are tiny (~30KB), so the system is optimized for request volume and tail latency, not raw bandwidth — this is a high-QPS, low-payload workload, closer to a chat API than a media-streaming service.

**Daily data volume**
```
56.25 GB audio (transient, discarded unless Cloud Sync is on)
+ formatted-text transcripts (~1KB/session avg)
  75,000 × 25 × 1KB ≈ 1.9 GB/day of durable transcript text (only for users with Private Cloud Sync on)
```
*Takeaway:* Because Privacy Mode/no-retention is a first-class option, durable storage volume is a small fraction of total traffic — most audio is processed and discarded, not archived *(architecture confirmed by Wispr's own [data controls page](https://wisprflow.ai/data-controls) and privacy policy)*.

**Storage sizing**
```
Assume 30% of MAU opt into Private Cloud Sync, 90-day rolling retention
500,000 MAU × 30% × 1.9GB/75,000 users/day × 90 days
≈ 150,000 users × ~25KB/day × 90 days
≈ 340 GB of retained transcript+metadata storage
```
*Takeaway:* This is a modest storage footprint even at scale — text is cheap; the hard engineering problem here is latency and correctness, not data volume.

**Read/write QPS**
```
75,000 DAU × 25 sessions/day ÷ 86,400s/day ≈ 22 dictation-completion writes/sec average
Assume 8x peak-to-average ratio (dictation clusters around work hours across timezones)
≈ 175 peak writes/sec for the inference pipeline
```
*Takeaway:* Peak QPS is well within range of a single well-provisioned autoscaled GPU serving fleet — this maps directly to why Baseten's autoscaling (rather than fixed GPU reservation) was the stated infra choice *(sourced: Baseten case study)*.

## 03. High-Level Architecture

```
┌────────────┐    ┌─────────────────┐    ┌──────────────────────┐    ┌───────────────┐
│  Client OS │ -> │ Local Capture &  │ -> │  Cloud Inference      │ -> │ Text Injection │
│  Overlay   │    │ VAD Preprocess   │    │  Pipeline (Baseten)   │    │ back into app  │
└────────────┘    └─────────────────┘    └──────────┬────────────┘    └───────────────┘
                                                       │
                                          ┌────────────┴────────────┐
                                          │  Fine-tuned Llama models │
                                          │  (TensorRT-LLM, Chains)  │
                                          │  on AWS, autoscaled GPU  │
                                          └────────────┬────────────┘
                                                       │
                                          ┌────────────┴────────────┐
                                          │ Optional: encrypted     │
                                          │ transcript store        │
                                          │ (Private Cloud Sync)    │
                                          └─────────────────────────┘
```

> **Why this shape?**
> - Client stays thin (capture + VAD + injection only) so the same core cloud pipeline serves four very different OS surfaces (Mac, Windows, iOS, Android) without duplicating the AI stack per platform.
> - Cloud-only inference is a deliberate tradeoff (not a limitation) — it's what makes context-aware LLM formatting possible at all, since on-device models couldn't match the fine-tuned Llama quality bar *(sourced: Wispr's own product blog)*.
> - Storage is opt-in and decoupled from the hot path — the default path (no Cloud Sync) never touches durable storage at all, keeping the common case both fast and low-liability.

**Request flow**
1. User speaks; OS-level overlay captures audio locally and runs basic voice-activity detection.
2. On pause/completion, the buffered audio (plus lightweight active-app/context signal) is sent to the cloud pipeline.
3. Baseten-hosted, fine-tuned Llama models (served via TensorRT-LLM engines, orchestrated through Baseten's Chains framework for the multi-step pipeline) transcribe and reformat the speech in one pass — not a live streaming transcript.
4. The formatted text token stream returns to the client (Wispr reports 100+ tokens generated in <250ms).
5. The client injects the finished text directly into the focused application's input field.
6. If Private Cloud Sync is enabled, the transcript is written to encrypted storage; otherwise it's discarded immediately after the response is delivered.

## 04. APIs & Contracts

Wispr Flow is a consumer/enterprise product, not a published developer platform — no public API was found in research, so this section models the **internal** client↔cloud contract rather than a third-party-facing API surface *(inferred — not publicly confirmed; no public API docs exist)*.

**Dictation endpoints (inferred internal contract)**
- `POST /v1/dictation/sessions` — open a dictation session, register active-app context
- `POST /v1/dictation/sessions/{id}/audio` — stream/submit captured audio chunk(s)
- `GET /v1/dictation/sessions/{id}/result` — poll or receive pushed formatted-text result
- `POST /v1/dictation/sessions/{id}/close` — finalize session, trigger optional Cloud Sync write

**Example: submit-and-receive dictation result** *(illustrative — not a confirmed public schema)*
```json
{
  "request": {
    "session_id": "sess_8f2a1c",
    "audio": "<base64 or streamed binary, ~30KB>",
    "context": {
      "active_app": "slack",
      "visible_text_snippet": "…redacted…",
      "privacy_mode": true
    }
  },
  "response": {
    "session_id": "sess_8f2a1c",
    "formatted_text": "Hey team — pushing the fix now, should be live in ~10 min.",
    "latency_ms": 612,
    "stored": false
  }
}
```

**Protocol notes**
- Idempotency matters here mainly for the audio-submit step (client retries on flaky mobile networks shouldn't double-transcribe) *(inferred — not publicly confirmed)*.
- `privacy_mode` / Zero Data Retention is a first-class request-level flag per Wispr's documented data controls, not an account-wide setting only — Privacy Mode is off by default for non-HIPAA users and must be explicitly enabled *(sourced: Wispr Flow data controls / security FAQ)*.

## 05. Data Model & Storage

**Session**
- PK  session_id (UUID)
- CK  user_id
- started_at, ended_at
- active_app (string)
- privacy_mode (bool)

**Transcript** *(only persisted when Private Cloud Sync is enabled)*
- PK  transcript_id (UUID)
- CK  session_id
- formatted_text (encrypted at rest)
- created_at, retention_expires_at

**User**
- PK  user_id (UUID)
- plan_tier (free / pro / enterprise)
- privacy_mode_default (bool)
- org_id (nullable, for Fortune 500/enterprise seats)

> **🔑 Critical Design Detail:** The retention model is the actual product differentiator here, not the schema shape. Wispr explicitly separates "Privacy Mode" (are we allowed to train on/store this at all) from "Cloud Sync" (do we persist transcripts for the user's own history) — Zero Data Retention is defined specifically as *both* being off simultaneously *(sourced: Wispr Flow data controls page)*. Any credible data model for this product has to represent that as two independent flags, not one binary "save or don't."

**Storage choices**
| Data type | Store | Why |
|---|---|---|
| In-flight audio | Ephemeral, in-memory/streaming buffer near the inference layer | Never durably stored by default; discarded post-request *(inferred implementation, confirmed behavior)* |
| Persisted transcripts (Cloud Sync users) | Encrypted-at-rest document/KV store | Small records, high read-by-user-id access pattern, encryption is a stated requirement, not the specific store *(inferred — specific database technology not publicly confirmed)* |
| User/account/org metadata | Relational store (assumed Postgres-class) | Standard for account/billing/org-hierarchy data at this scale *(inferred — not publicly confirmed)* |

**Partitioning & access patterns**
- Partition key: user_id (or org_id for enterprise multi-seat accounts) — nearly all reads/writes are scoped to "this user's own history."
- Dominant access patterns:
  1. Write-once-per-session transcript insert (Cloud Sync path only)
  2. Read-by-user-id for in-app dictation history
  3. Bulk delete-by-retention-expiry (background job enforcing the configured retention window)

## 06. Audio Capture & OS Integration

Flow runs as an OS-layer overlay rather than embedding inside individual apps — described in its own product writing as "a layer that runs across them all: email, Slack, Notion, iMessage, ChatGPT, etc." *(sourced: Wispr Flow blog, "Designing a natural and useful voice interface")*.

```
[OS Accessibility/Input Layer] <-> [Wispr Overlay Process] <-> [Focused App's text field]
```

- Local voice-activity detection (VAD) determines start/stop of a dictation turn before anything is sent to the cloud.
- A persistent, low-attention UI element ("a small, persistent bar on the dock") signals recording state without being visually intrusive.
- The overlay must integrate with OS-level accessibility/input APIs to inject text into an arbitrary focused field — this is the hardest cross-platform engineering problem in the whole system, not the ML.

> **Tradeoff:** Building a true OS-level overlay (vs. a per-app plugin/extension model) means far more platform-specific engineering work, but it's what lets Flow work identically inside Slack, an IDE, or a native mobile app — a plugin-per-app model would never reach that breadth.

## 07. LLM Post-Processing Pipeline (Not Streaming ASR)

This is the architectural decision most worth interview time.

```
Audio ──> [Speech Encoder] ──> [Fine-tuned Llama: transcribe + clean + format] ──> Final Text
                (no intermediate raw-transcript UI shown to user)
```

- Traditional dictation tools stream a raw ASR transcript token-by-token, visibly correcting itself as you speak. Flow deliberately withholds output until the LLM pass completes, explicitly described as choosing to "wait, understand, and then write what you *meant*" rather than what was literally said *(sourced: Wispr Flow blog)*.
- This single pass handles transcription, grammar correction, tone-matching, and (over time) adaptation to a user's personal phrasing style together, rather than as separate pipeline stages *(sourced: Wispr Flow blog)*.

> **Tradeoff:** Buffer-then-format vs. stream-then-correct. Streaming feels more "live" but produces visibly messy intermediate text and more user-side correction. Flow's buffered approach adds a few hundred ms of perceived latency (justifying the aggressive p99 < 700ms target) in exchange for output that's usually correct on the first render.

## 08. Context-Aware Formatting

- The client detects the active application and relevant visible content and passes that as context alongside the audio *(sourced: research summary of Wispr's architecture)*.
- The same underlying LLM pass uses that context to decide output shape: an email destination produces email-formatted prose, a code-editor destination produces a properly structured code comment.

> **Tradeoff:** Sending screen/app-context metadata to the cloud (not just raw audio) increases what's "personal/sensitive" about each request — which is precisely why Privacy Mode / Zero Data Retention exists as a first-class, easily-toggled control rather than an afterthought.

## 09. Model Serving on Baseten/AWS

★ **Key Page**

```
Client Request ──> Baseten Model Endpoint (AWS us-east-1) ──> TensorRT-LLM engine
                                                    │
                                          Baseten "Chains" framework
                                          orchestrates multi-step
                                          inference (transcribe -> format)
```

- Fine-tuned Meta Llama models, chosen specifically for "complete ownership over their AI systems" rather than depending purely on a third-party foundation-model API *(sourced: Baseten case study)*.
- TensorRT-LLM used as the inference engine builder for the fine-tuned models; Baseten's Chains framework orchestrates the multi-step pipeline (transcribe → clean → format) as a single served unit rather than several hand-wired services.
- GPU capacity autoscales for traffic spikes rather than holding permanent reservations — directly addresses the ~175 peak-QPS estimate from Section 02 without idle GPU cost during off-peak hours.
- The company explicitly optimizes for p99, not median latency ("we're optimizing the p99 experience for the p99 user") — a notable departure from the median-latency-first framing common in most system-design answers.

> **Tradeoff:** Fine-tuning and self-hosting (via Baseten) vs. calling a foundation-model API directly. Self-hosting costs more engineering effort but buys latency control (100+ tokens in <250ms), customization depth, and avoids being fully dependent on a third party for the core product experience — though research also surfaced that OpenAI and Meta models have been used as infrastructure partners at various points, suggesting a hybrid/multi-provider strategy rather than single-vendor lock-in *(inferred — exact current model-provider mix not fully confirmed)*.

## 10. Privacy Modes & Zero Data Retention

- **Privacy Mode**: controls whether dictation data can be used to evaluate/train/improve AI models (by Wispr or any third party). Off by default for non-HIPAA users *(sourced: Wispr Flow security/compliance FAQ)*.
- **Private Cloud Sync**: controls whether transcripts are durably stored (encrypted at rest/in transit) vs. processed in real time and discarded after the request completes.
- **Zero Data Retention (ZDR)** = Privacy Mode on **and** Cloud Sync off, simultaneously — no training use, no server-side storage at all.
- Compliance posture: SOC 2 Type II certified, HIPAA controls available on all plans *(sourced: Baseten case study, Wispr security FAQ)*.

> **💡 Interview Tip:** This is a good place to demonstrate PM-level thinking, not just engineering: the retention model isn't just a technical toggle, it's a segmentation strategy — HIPAA/enterprise customers get privacy-by-default, prosumer users get convenience-by-default (searchable dictation history), and the same underlying architecture serves both without a separate deployment.

## 11. Cross-Platform Divergence (Android vs. iOS vs. Desktop)

- **Android**: floating overlay/bubble sits above every app — a deliberate choice to avoid Android's fragmented keyboard-replacement ecosystem *(sourced: Wispr Flow blog / research summary)*.
- **iOS**: implemented closer to a keyboard-extension pattern, aligning with Apple's stricter platform conventions for system-wide input *(sourced: research summary)*.
- **Desktop (Mac/Windows)**: full OS-level accessibility-layer integration, injecting text into essentially any focused field.

> **Tradeoff:** A single "floating bubble" UI everywhere would be simplest to build, but iOS platform constraints make that pattern awkward there — so Flow accepts extra platform-specific engineering to respect each OS's conventions rather than forcing one UI paradigm across all four platforms.

## Scaling & Caching

**Cache hierarchy:** Given the workload is short, unique, per-user audio, there's little to cache on the inference path itself (each request is novel speech) — the more relevant caching is at the model-serving layer: warm GPU model replicas kept ready via Baseten's autoscaling rather than cold-starting per request *(inferred implementation detail; autoscaling behavior itself is sourced from the Baseten case study)*.

**Hot-key mitigation:** Not a classic hot-partition problem — traffic is naturally spread across millions of independent users/sessions rather than concentrated on a few keys. The closer analog is *regional* load concentration during business hours across timezones, addressed by autoscaling GPU capacity rather than data-layer sharding.

**Sharding:** Persisted transcript/account data would naturally shard by user_id/org_id given the access pattern established in Section 05 *(inferred — not publicly confirmed)*.

**Multi-region:** Wispr's infrastructure is described as supporting multi-region deployments for compliance and latency reasons *(sourced: Baseten case study)*; primary inference is documented as running in AWS us-east-1, with other regions likely used for latency-sensitive or data-residency-constrained customers *(inferred beyond the confirmed us-east-1 detail)*.

## Failures & Reliability

| Failure Mode | Mitigation |
|---|---|
| Cloud inference endpoint unavailable/slow | Client should degrade to a clear "unavailable" state rather than silently failing — cloud-only architecture means there's no local fallback transcription *(inferred — not publicly confirmed as implemented)* |
| GPU capacity spike beyond autoscaling headroom | Baseten's autoscaling is the documented mitigation; presumably backed by request queuing/backpressure at the edge *(partially inferred)* |
| Text injected into wrong/unintended field (focus changed mid-dictation) | OS-level focus-tracking must re-validate target field before injection — a documented UX/engineering challenge given the cross-app injection model |
| Sensitive context leaked via screen-context capture | Privacy Mode / ZDR exists specifically to bound this risk; enterprise HIPAA customers get privacy-by-default posture |
| Third-party model-provider outage/degradation (if using OpenAI/Meta-hosted infra alongside self-hosted Baseten models) | Multi-provider strategy (self-hosted fine-tuned Llama + third-party partners) plausibly provides some redundancy, though this is not confirmed as an explicit failover design *(inferred)* |

**Degradation ladder:** Full LLM formatting → (if degraded) fall back toward faster/cheaper model tier → (if fully unavailable) clear "dictation unavailable" state, since there's no offline path by design *(inferred — not publicly confirmed)*.

**Client resilience:** Local VAD/buffering should tolerate brief network blips without dropping in-progress audio; retries on the audio-submit step should be idempotent per session (see Section 04 API notes) *(inferred)*.

**What to monitor:** p99 end-to-end latency (the company's own headline metric), GPU utilization/autoscaling lag, transcription accuracy drift by device/mic type, injection-failure rate (text landing in the wrong field).

## Recap & Interview Cheat Sheet

**The whole system in one line:** A thin, OS-level capture client feeds short audio bursts to a fine-tuned-Llama inference pipeline (self-hosted on Baseten/AWS for latency and model control) that transcribes *and* formats in one LLM pass, with storage being opt-in rather than default.

```
Client Overlay -> Cloud LLM Pipeline (Baseten/AWS, autoscaled) -> Formatted Text -> Injected back
                                    |
                         Optional encrypted transcript store (Cloud Sync only)
```

**Top talking points**
- Buffer-then-format beats stream-then-correct for this product's UX goals — explicitly a latency-for-correctness tradeoff.
- p99, not median, is the latency metric the company optimizes for.
- Self-hosted fine-tuned models (via Baseten) trade engineering effort for latency control and independence from a single foundation-model vendor.
- Privacy Mode and Cloud Sync are two independent flags, not one — this is the real product/data-model insight, not the entity schema.
- The hardest engineering problem is OS-level, cross-app text injection — not the ML.
- Storage is opt-in and small relative to total traffic, because most audio is processed and discarded.

**Quick Q&A**
- Q: Why not stream ASR results like most dictation tools? — A: Streaming ASR shows visibly-wrong intermediate text; buffering lets the LLM correct for *meaning* before anything renders.
- Q: How do they hit sub-second latency with an LLM in the loop? — A: Self-hosted fine-tuned models on dedicated, autoscaled GPU infra (TensorRT-LLM via Baseten) instead of a general-purpose hosted API.
- Q: What happens to my audio? — A: Discarded after processing by default; only persisted (encrypted) if the user explicitly enables Private Cloud Sync.
- Q: Why is this cloud-only instead of on-device like some competitors? — A: The LLM-based formatting quality bar isn't achievable on-device today; cloud-only is a deliberate quality tradeoff, not an oversight.

**Numbers to remember**
- p99 end-to-end latency: <700ms; 100+ tokens generated in <250ms
- ~2.5M downloads, 270 Fortune 500 customers, 70% 12-month retention
- $10M ARR (2025); valuation moved from ~$700M (Nov 2025) toward reported $2B talks (2026)

**Mistakes to avoid**
- Don't default to describing this as "just a Whisper API wrapper" — the differentiated architecture is the buffered LLM-formatting pass and the self-hosted serving stack, not the ASR step alone.
- Don't skip the privacy/retention model — it's a first-class architectural component here, not a bolt-on compliance checkbox.
- Don't assume streaming transcription is strictly better — this product's core bet is the opposite.
- Don't over-invent exact database technologies or region counts as if confirmed; several implementation specifics here are reasonable inference, not public fact (flagged throughout).

**Flow vs. Superwhisper (closest architectural counter-example)**
| | Wispr Flow | Superwhisper |
|---|---|---|
| Processing location | Cloud-only | On-device (offline-capable) |
| Formatting intelligence | LLM post-processing pass | Lighter local model, less context-aware formatting |
| Compliance posture | SOC 2 Type II, HIPAA controls | Not highlighted publicly |
| Core tradeoff | Higher formatting quality, requires network | Full privacy/offline, less contextual formatting |
